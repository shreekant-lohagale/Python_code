# Import the sum_of_numbers function from the math_operations package
from pra8_2 import sum_of_numbers

# Call the function
result = sum_of_numbers(10, 20)
print(f"The sum of the numbers is: {result}")
